#Create a method that takes as input a name, city,
#  and state to welcome a person.
#  Note that name will be an array consisting of 
# one or more values that should be joined together with one space between each,
#  and the length of the name array in test cases will vary.
def even_or_odd(number):
    if number%2 ==0:
        return("Even")
    else:
        return("Odd")